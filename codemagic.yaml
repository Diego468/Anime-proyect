workflows:
  build:
    name: Build APK
    environment:
      flutter: true
    scripts:
      - name: Gradle build
        script: |
          ./gradlew clean assembleRelease  # Cambia a assembleRelease si deseas una versión de lanzamiento
    artifacts:
      - app/build/outputs/apk/debug/*.apk  # Cambia según el tipo de APK que estés generando
